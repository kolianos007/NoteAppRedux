const initialState = {
  note: [],
};

const createNoteReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default createNoteReducer;
